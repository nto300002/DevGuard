{
  "name": "agent-safecheck",
  "version": "0.1.10",
  "description": "AI開発脆弱性予防のためのpre-commit / pre-push self-review CLI.",
  "type": "module",
  "bin": {
    "safecheck": "dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "prepublishOnly": "npm run typecheck && npm test && npm run build"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "vitest": "^2.1.8"
  },
  "dependencies": {
    "php-parser": "^3.7.0",
    "typescript": "^5.9.3",
    "yaml": "^2.9.0"
  }
}
